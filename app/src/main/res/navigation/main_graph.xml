<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_graph"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="nl.jpelgrom.huetemp.ui.MainFragment"
        android:label="main_fragment"
        tools:layout="@layout/fragment_main" >
        <action
            android:id="@+id/action_addBridge"
            app:destination="@id/addBridgeFragment" />
    </fragment>
    <dialog
        android:id="@+id/addBridgeFragment"
        android:name="nl.jpelgrom.huetemp.ui.AddBridgeFragment"
        android:label="fragment_add_bridge"
        tools:layout="@layout/fragment_add_bridge" />
</navigation>